📱 WhatsApp Number Matcher

Этот Python-скрипт предназначен для сопоставления номеров телефонов между двумя CSV-файлами:

основным списком контактов (contacts_merged.csv),

и файлом с результатами проверки наличия WhatsApp (results.csv).

Скрипт автоматически нормализует номера, ищет совпадения и добавляет в итоговый CSV-файл колонку с пометкой —
✅ есть ватсап или ❌ нету.

🚀 Возможности

📂 Автоматическое определение колонок с телефонами и статусом в файле WhatsApp-результатов.

📞 Нормализация номеров телефонов к формату +7XXXXXXXXXX.

🔍 Сравнение и маркировка контактов в основном файле.

⚡ Работа с большими CSV без зависаний.

🧠 Отладочная информация — примеры несовпадений, предварительный просмотр и статистика.

📦 Установка зависимостей
pip install tqdm


(модуль csv и os — встроенные в Python, устанавливать не нужно)

🧩 Использование

Подготовь два файла:

contacts_merged.csv — основной файл со всеми контактами (обязательно содержит колонку с номерами, например phone2);

results.csv — результат выгрузки проверки WhatsApp (в нём есть колонки с телефоном и статусом).

При необходимости измени настройки в начале скрипта:

INPUT_CSV_FILE = 'contacts_merged.csv'   # основной CSV с контактами
WHATSAPP_RESULTS_CSV = 'results.csv'     # файл с результатами проверки
OUTPUT_CSV_FILE = 'numb1.csv'            # имя выходного файла
PHONE_COLUMN_NAME = 'phone2'             # колонка с телефоном в основном файле
NEW_COLUMN_NAME = 'WA'                   # название новой колонки
WA_CSV_POSITIVE_STATUS = 'ДА'            # значение, обозначающее "есть WhatsApp"


Запусти скрипт:

python match_whatsapp.py


Дождись завершения и посмотри результат:

✅ Загружено 8243 уникальных номеров с WhatsApp
✅ Найдено 15678 номеров в основном файле
✅ Готово! Результат сохранён в numb1.csv

📊 Результат

В файл numb1.csv добавляется новая колонка:

phone2	name	WA
+79991234567	Иван Иванов	есть ватсап
+79161234567	Анна Петрова	нету
...	...	...
⚙️ Как работает

Парсит файл results.csv, определяет колонки для телефона и статуса.

Нормализует номера в формате +7XXXXXXXXXX.

Собирает список номеров, где статус = "ДА" (или другое указанное значение).

Открывает основной CSV и добавляет новую колонку с пометкой WhatsApp.

Пишет итог в numb1.csv с сохранением структуры исходного файла.

🧠 Подсказки

Если в файле results.csv названия колонок неочевидные — скрипт сам попробует их определить (ищет слова вроде phone, телефон, status, whatsapp и т.д.).

Если не находит — выведет несколько первых строк файла, чтобы помочь вручную указать имена колонок.

Форматирует телефоны, удаляя лишние символы (скобки, пробелы, дефисы).

Работает с любым порядком колонок и не требует предобработки файлов.

📝 Лицензия

MIT License — свободно использовать, модифицировать и распространять.
